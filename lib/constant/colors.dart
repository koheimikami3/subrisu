import '../importer.dart';

// アプリカラー
const appColor = Color.fromARGB(255, 0, 200, 180);

// ライトモード時の画面背景色
const lightBackgroundColor = Color.fromRGBO(240, 240, 245, 1);

// ダークモード時の画面背景色
const darkBackgroundColor = Color(0xFF212121);

// ダークモード時の項目色
const darkItemColor = Color(0xFF303030);
